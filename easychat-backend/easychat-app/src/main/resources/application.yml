server:
  port: 8080

spring:
  application:
    name: easychat
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://gz-cdb-ns0ptu89.sql.tencentcdb.com:26090/ai-chat?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: aichat
    password: Denglinyi0453
  # Elasticsearch config is optional.
  # elasticsearch:
  #   uris: http://localhost:9200
  druid:
    url: jdbc:mysql://gz-cdb-ns0ptu89.sql.tencentcdb.com:26090/ai-chat?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: aichat
    password: Denglinyi0453
    driver-class-name: com.mysql.cj.jdbc.Driver

    initial-size: 5
    min-idle: 5
    max-active: 20
    max-wait: 60000

    validation-query: SELECT 1
    test-while-idle: true

    filters: stat,wall,slf4j

    stat-view-servlet:
      enabled: true
      login-username: admin
      login-password: admin

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.log4j2.Log4j2Impl
  global-config:
    db-config:
      id-type: auto

# LangChain4j LLM config
easychat:
  llm:
    api-key: ${OPENAI_API_KEY:sk-31cf248a9db24800a7e769e106ccdcc8}
    base-url: ${OPENAI_BASE_URL:https://api.deepseek.com/v1}
    model-name: ${OPENAI_MODEL:deepseek-chat}
    temperature: 0.7
    max-tokens: 2000
